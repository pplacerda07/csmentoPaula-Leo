{
    "name": "lpcasamento-root",
    "version": "1.0.0",
    "description": "Root package manager for the wedding site",
    "scripts": {
        "dev": "npm --prefix site run dev",
        "build": "npm --prefix site run build",
        "preview": "npm --prefix site run preview",
        "install-all": "npm --prefix site install"
    }
}